bool p;
bool q;
int c;

void print_c() {
    cout << c + 1;
    cout << "\n";
}

int main() {
  int a;

  a = 2;

  if(true) {
    int b;
    b = 6;
    while (b >= 0) {
        a = c = 3 + a;
        b--;
    }
    cout << b;
    cout << "\n";
  }
  print_c();
  cout << a;
  cout << "\n";

  p = q = true;

  cout << ((p || q) && !(a != c));

  return c;
}